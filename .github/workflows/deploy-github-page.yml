name: deploy-github-page
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          token: ${{secrets.ACCESS_TOKEN}}
      - run: docker-compose run build-page
      - run: cd /_build/html
      - uses: EndBug/add-and-commit@v7
        with:
          add: '/* --force'
          branch: 'gh_page'
          message: 'deploy'
          push: 'origin gh_page --force'
